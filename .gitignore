# Secrets — NIEMALS committen
.env
.env.local
Config/Secrets.xcconfig
*.p12
*.p8
*.mobileprovision
*.cer
tools/.token.json

# Xcode (Projekt wird von XcodeGen erzeugt)
StudGo.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
build/
*.ipa
*.dSYM.zip

# System
.DS_Store

# Arbeitsdokumente, die nicht öffentlich gehören
__pycache__/
docs/mail-*.md

# Zertifikats-Schlüssel für die iOS-Signatur — niemals committen
.secrets/

# SwiftPM — lokales Übersetzen und Testen von StudGoKit
.build/
.swiftpm/
.swiftpm-home/

# App-Store-Aufnahmen (Marketing, nicht im Quelltext gebraucht)
store/
