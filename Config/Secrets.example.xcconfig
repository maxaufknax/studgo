// Vorlage — echte Werte stehen in Config/Secrets.xcconfig (gitignored).
// Erzeugen mit: ./tools/bootstrap-secrets.sh
// Client 17 ist ein public client (PKCE-only) — ein Secret gibt es nicht mehr.
STUDIP_CLIENT_ID = 17
